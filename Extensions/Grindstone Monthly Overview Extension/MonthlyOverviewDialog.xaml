<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Monthly Overview" Height="600" Width="1200"
        WindowStartupLocation="CenterOwner"
        Background="White" FontFamily="Segoe UI">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / Selection -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Month:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="monthComboBox" Width="120" Margin="0,0,15,0"/>
            
            <TextBlock Text="Year:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="yearComboBox" Width="80" Margin="0,0,15,0"/>

            <Button x:Name="refreshButton" Content="Refresh" Padding="10,2" Margin="0,0,15,0"/>
            
            <TextBlock Text="Total Workdays: " VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBlock x:Name="totalWorkdaysText" Text="0" VerticalAlignment="Center" Margin="0,0,15,0"/>
            
            <TextBlock Text="Average Hours/WorkDay: " VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBlock x:Name="averageHoursText" Text="0.00" VerticalAlignment="Center"/>

            <TextBlock Text="Overtime: " VerticalAlignment="Center" FontWeight="Bold" Margin="15,0,0,0"/>
            <TextBlock x:Name="overtimeText" Text="0.00" VerticalAlignment="Center"/>

            <TextBlock x:Name="statusText" VerticalAlignment="Center" Foreground="Gray" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" x:Name="overviewGrid" 
                  AutoGenerateColumns="True" 
                  IsReadOnly="True"
                  SelectionUnit="Cell"
                  SelectionMode="Single"
                  HeadersVisibility="All"
                  GridLinesVisibility="All"
                  HorizontalGridLinesBrush="#E0E0E0"
                  VerticalGridLinesBrush="#E0E0E0"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="#F8F8F8"
                  BorderBrush="#D0D0D0"
                  BorderThickness="1">
             <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="Background" Value="#F0F0F0"/>
                    <Setter Property="BorderBrush" Value="#D0D0D0"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="4,2"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="{TemplateBinding BorderThickness}" 
                                        Background="{TemplateBinding Background}" 
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style TargetType="DataGridRow">
                     <Style.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#61a9e0" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#61a9e0" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White" />
                    </Style.Resources>
                </Style>
            </DataGrid.Resources>
        </DataGrid>

        <!-- Footer -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="closeButton" Content="Close" Padding="15,5"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
